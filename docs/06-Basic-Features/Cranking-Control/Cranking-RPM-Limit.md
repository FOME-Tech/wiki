# Cranking RPM Limit

The cranking RPM limit informs the ECU to transition out of cranking mode once the engine RPM exceeds the set limit. Typically values in the 350-450 RPM range are reasonable values.
